# V3の開発方針

- V1・V2の要件や実装を根拠なく前提にしない。採用前に現在の目的、成立条件、より簡単な代案を確認し、主要判断は `docs/design.md` に記録する。
- 互換層や旧実行器への委譲を作らず、V3単独で動く構造を保つ。
- 動作固有の制御コードを増やす前に、同じクリップ形式と区間の構成で表現できるか検討する。
- 見本の固定版、使用箇所の編集、現在状態からの接続を中心にする。構成の階層は必要性が実例で示されるまで増やさない。
- 数値的に実行できること、意味の適切さ、人が特定の身体・場面で採用したことを分ける。人の評価を生成して埋めない。
- 元に戻しやすい小変更、文言・配置、代入や分岐をなぞるテストは追加しない。軌道の連続性、保存データの不変性、競合、外部操作の境界など、成果を守る意味のある検証に絞る。
- 動作や画面の変更は必要な実速度・実モデルの確認を行う。既存のチェックが通った後の重複実行を目的にしない。
