{
  "name": "prometheus-v3",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=24.0.0" },
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "start": "npm run build && node dist/server.js",
    "dev": "npm run build && node --watch dist/server.js",
    "test": "node --test tests/*.test.ts",
    "motion": "node src/cli.ts",
    "mcp": "node src/mcp.ts"
  },
  "dependencies": {
    "@pixiv/three-vrm": "3.5.5",
    "three": "0.185.1",
    "zod": "4.5.4"
  },
  "devDependencies": {
    "@types/node": "24.3.0",
    "@types/three": "0.185.4",
    "typescript": "5.9.2"
  }
}
